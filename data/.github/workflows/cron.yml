name: Wake & Run Scheduler

on:
  schedule:
    - cron: "*/5 * * * *"   # çdo 5 min
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call /cron/run
        run: |
          curl -sS --max-time 25 https://insta-scheduler-server.onrender.com/cron/run || true
